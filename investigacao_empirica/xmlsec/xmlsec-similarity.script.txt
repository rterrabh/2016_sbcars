| methods classes models mooseModel mooseClasses mooseMethods |methods := { 'outputPItoWriter' . 'outputCommentToWriter' . 'outputPItoWriter' . 'canonicalizeSubTree' . 'outputCommentToWriter' . 'outputTextToWriter' . 'getPositionRelativeToDocumentElement' . 'outputTextToWriter' . 'outputAttrToWriter' . 'outputAttrToWriter' . 'canonicalizeXPathNodeSet' . 'getPositionRelativeToDocumentElement'} asSet.classes := { 'Canonicalizer20010315' . 'Canonicalizer20010315Excl' . 'CanonicalizerBase'} asSet.models := { 'xml-security-1' . 'xml-security-2' }.models do: [ :modelName |	mooseModel := (MooseModel root select: [ :each | each name = modelName]) anyOne.	mooseClasses :=  mooseModel allModelClasses select: [ :each | classes contains: [:el | el = each name]].	mooseMethods := mooseModel allModelMethods select: [ :each | methods contains: [:el | el = each name]].	BlockBasedVisualizations measureStructuralSimilarityIn: mooseMethods of: mooseClasses.].